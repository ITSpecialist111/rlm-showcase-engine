# Name: Audit Status
kind: AdaptiveDialog
beginDialog:
  kind: OnRecognizedIntent
  id: AuditStatus
  intent:
    displayName: AuditStatus
    includeInOnSelectIntent: false
    triggerQueries:
      - audit status
      - check audit status
      - status
  actions:
    - kind: SendActivity
      id: send-status
      activity:
        text:
          - "Stub: get_audit_status action exists. Configure connector and dialog bindings in Copilot Studio."
